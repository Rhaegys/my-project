{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Hello {{ data }}! ✅</h1>
    </div>
    <div class="container">
        <a href="{{ path('instrument_index') }}" class="btn btn-outline-primary">Your Assets</a>
        <br>
        <a href="{{ path('data') }}" class="btn btn-outline-primary">Show Assets' Worth</a>
    </div>
{% endblock %}
